[package]
name = "rendering"
version = "0.1.0"
edition = "2021"

[dependencies]
application = { path = "../application" }
window = { path = "../window" }
legion = "0.4.0"
wgpu = "0.19.3"
bytemuck = {version = "1.15.0", features = ["derive"]}
